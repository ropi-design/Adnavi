# Flux UI エラー修正手順

Flux UI のエラーを修正するために、以下のコマンドを手動で実行してください：

```bash
# Laravel Sailの場合
./vendor/bin/sail artisan flux:install

# 通常の環境の場合
php artisan flux:install
```

これで Flux UI コンポーネントが使用できるようになります。
